spring:
  datasource:
    url: jdbc:mysql://localhost:3306/marketplace_db_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.marketplace: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

